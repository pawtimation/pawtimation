import { runDailyDigest } from './digest.js';import { getAgentFlags } from './controller.js';let timers=[];export function startAgents(){stopAgents();const {digest}=getAgentFlags();if(digest){const every=Number(process.env.DIGEST_INTERVAL_MS||60000);timers.push(setInterval(()=>runDailyDigest().catch(console.error),every))}console.log('[agents] running',getAgentFlags())}export function stopAgents(){timers.forEach(clearInterval);timers=[]}
