Staff Dashboard Must Show Live Job Data

The Staff Dashboard (/staff) must display:
	•	Upcoming jobs for the logged-in staff member
	•	Count of today’s jobs
	•	Quick stats based on assigned bookings
	•	Alerts for:
	•	New job assigned
	•	Job updated
	•	Route added by admin

Requirements:
	•	Use the same data source that Admin uses, but filtered by staffId.
	•	All numbers and lists must update via socket events when bookings change.

⸻

2️⃣ Fix Staff Calendar (Currently Empty)

The staff weekly calendar (/staff/calendar) MUST:
	•	Load bookings assigned to the logged-in staff member
	•	Display them in the same weekly grid used by admin
	•	Hide all unassigned or other-staff bookings
	•	Show the 🗺️ route icon when a job has a walking route attached
	•	Enable click-to-open job detail view

Requirements:
	•	The staff calendar must use:
GET /jobs?staffId={loggedInStaffId}
	•	If necessary, add filtering server-side.

⸻

3️⃣ Fix Staff “My Jobs” List (Currently Blank)

“My Jobs” (/staff/jobs) should display:
	•	Jobs grouped into:
	•	Upcoming
	•	Today
	•	Completed
	•	Quick-tag indicators:
	•	Time
	•	Duration
	•	Dog(s)
	•	Route available (🗺️)

Requirements:
	•	Must use the same API the admin uses, but filtered by staffId.
	•	Route icon showing only when booking has route data saved.

⸻

4️⃣ Staff Job Detail Screen — MUST EXIST & MATCH ADMIN

This is the MOST IMPORTANT staff page.

Staff Job Detail should include:

Job Info
	•	Time
	•	Duration
	•	Client name + address
	•	Dogs
	•	Notes
	•	Service type

Route Section (View-Only)
	•	Show the walking route generated by admin
	•	Display:
	•	Mini map preview
	•	Distance
	•	Estimated duration
	•	“Start Navigation” button (opens Google Maps URL)

Requirements:
	•	Staff cannot generate or edit routes — read-only.
	•	Admin-generated routes MUST automatically appear for staff.

⸻

5️⃣ Staff Availability Screen MUST Load + Save

“My Availability” (/staff/availability) must:
	•	Load the weekly availability from backend
	•	Allow staff to update their weekly hours
	•	Allow adding exceptions (holidays, booked off days)
	•	Display calendar of unavailable dates

Requirements:
	•	Ensure GET + POST endpoints are correctly connected
	•	Ensure businessId and staffId are passed each call

⸻

6️⃣ Walking Route Integration for Staff

Staff UI must support the new walking-route engine:

Staff Should See:
	•	Route preview
	•	Map image (static)
	•	Walk distance and estimated time
	•	“Start Navigation” button
	•	Route icon on job and calendar screens

Staff Should NOT:
	•	Generate routes
	•	Edit routes
	•	Delete routes

Staff = field worker, not planner.

⸻

7️⃣ Staff Settings Page Must Function

Staff Settings (/staff/settings) should allow:
	•	Staff display name
	•	Contact details
	•	Notification preferences
	•	Profile photo upload (if available in system)

Requirements:
	•	Must pull real data from /me
	•	Must save to staff profile update endpoint

⸻

8️⃣ Role-Specific Access Control

Ensure the following:
	•	Staff users cannot access admin or client pages
	•	Staff routed automatically to /staff/dashboard after login
	•	LocalStorage user object updated correctly on profile changes

⸻

9️⃣ UX Consistency

All Staff UI:
	•	Must use business branding theme
	•	Should use the new primary colour + optional logo
	•	Should display “Powered by Pawtimation” if enabled in settings
	•	Should match the minimalist admin mobile styling

⸻

🔟 Final Validation Checklist for Staff UI

Replit must verify:

Data Flows
	•	Staff calendar displays correct bookings
	•	Staff job list loads all assigned jobs
	•	Job detail loads route information
	•	Availability saves & loads correctly

Route Integration
	•	Route preview works
	•	“Start Navigation” opens correctly
	•	Route icon shows in calendar + job list

Permissions
	•	Staff cannot see admin routes
	•	Staff cannot generate/edit routes

Branding
	•	Correct theme/colour applied
	•	Logo appears where defined

⸻

✔️ Instruction for Completion

Return output ONLY when ALL staff UI items above are implemented, connected, tested, and working end-to-end for a real staff login.