Awesome—let’s make your demo sitter Becci, add a clickable month calendar (toggle unavailable dates), and show a locality map driven by postcode/city. No new npm deps, no API keys.

Below is a single paste-ready patch for your Replit Agent. It only adds small files/edits and keeps everything compatible with your current app.

⸻

🧩 Paste this patch to Replit Agent (one message)

PATCH ONLY. British English.
Add: demo sitter “Becci”, postcode field, month calendar picker on the Edit screen, and a map embed that follows the sitter’s postcode/city.

1) API: make the demo sitter “Becci” + add postcode

Replace or update apps/api/src/sitterRoutes.js so the ensure(id) default looks like this (only the object values changed/extended):

function ensure(id) {
  if (!sitters.has(id)) {
    sitters.set(id, {
      id,
      name: 'Becci',                     // <- demo name
      city: 'Aylesbury',
      postcode: 'HP20',                  // <- NEW
      bio: 'Friendly, reliable and fully insured. I love countryside walks and calm, positive training.',
      avatarUrl: 'https://placehold.co/256x256?text=Becci',
      bannerUrl: 'https://placehold.co/1200x400?text=Pawtimation',
      yearsExperience: 2,
      verification: { email: true, sms: true, stripe: true, trainee: false, pro: true },
      services: [
        { key: 'daycare',   label: 'Doggy Day Care',        price: 45, per: 'day',  extraPet: 5,  at: 'sitter' },
        { key: 'walk',      label: 'Dog Walking',           price: 25, per: 'walk', extraPet: 10, at: 'owner'  },
        { key: 'homevisit', label: 'One home visit a day',  price: 25, per: 'day',  at: 'owner'  }
      ],
      canAccept: { puppies: true, ages: 'All ages', sizes: 'All sizes' },
      cancellation: { type: 'moderate', copy: 'Full refund if you cancel before 12:00pm, 7 days prior.' },
      availability: { updatedAt: Date.now(), unavailable: ['2025-10-21'] }, // ISO dates
      gallery: [],
      rating: 4.9,
      reviews: 12,
      social: {
        instagram: { handle: '@becci.dogs', connected: false, autoPost: false },
        tiktok:    { handle: '@becci.dogs', connected: false, autoPost: false },
        x:         { handle: '@becci_dogs', connected: false, autoPost: false }
      }
    });
  }
  return sitters.get(id);
}

If sitterRoutes.js already exists from earlier, just update the default object and add the postcode line.

⸻

2) WEB: lightweight Month Calendar (no dependencies)

Create apps/web/src/components/MonthCalendar.jsx:

import React from 'react';

// utils
function ymd(date){ return date.toISOString().slice(0,10); }
function daysInMonth(y,m){ return new Date(y, m+1, 0).getDate(); }

export default function MonthCalendar({ year, month, selected=[], onToggle }){
  const first = new Date(year, month, 1);
  const startWeekday = (first.getDay()+6)%7; // Monday=0
  const total = daysInMonth(year, month);
  const cells = [];
  for (let i=0;i<startWeekday;i++) cells.push(null);
  for (let d=1; d<=total; d++) cells.push(new Date(year, month, d));

  const selectedSet = new Set(selected);

  return (
    <div className="border rounded-xl p-3">
      <div className="grid grid-cols-7 gap-1 text-xs text-slate-500 mb-1">
        {['Mon','Tue','Wed','Thu','Fri','Sat','Sun'].map(x=><div key={x} className="text-center">{x}</div>)}
      </div>
      <div className="grid grid-cols-7 gap-1">
        {cells.map((dt, i)=>{
          if (!dt) return <div key={'e'+i} />;
          const key = ymd(dt);
          const isSel = selectedSet.has(key);
          return (
            <button
              key={key}
              onClick={()=>onToggle?.(key, !isSel)}
              className={
                'h-9 rounded text-sm ' +
                (isSel ? 'bg-rose-600 text-white' : 'bg-slate-100 hover:bg-slate-200')
              }>
              {dt.getDate()}
            </button>
          );
        })}
      </div>
      <div className="text-xs text-slate-500 mt-2">
        Tap a date to toggle **unavailable**. Selected dates show in red.
      </div>
    </div>
  );
}


⸻

3) WEB: update SitterEdit to include name “Becci”, postcode, calendar (+ month nav), and a locality map

Edit apps/web/src/screens/SitterEdit.jsx:
	•	Add import at the top:

import MonthCalendar from '../components/MonthCalendar.jsx';

	•	Inside the component, add month/year state:

const today = new Date();
const [calYear, setCalYear] = useState(today.getFullYear());
const [calMonth, setCalMonth] = useState(today.getMonth()); // 0-11

	•	In the Basics section, add a Postcode input (alongside City):

<div className="grid md:grid-cols-3 gap-3">
  <div className="md:col-span-1">
    <label className="text-sm">Display name</label>
    <input className="border rounded w-full px-3 py-2" value={s.name} onChange={e=>setS({...s, name:e.target.value})}/>
  </div>
  <div>
    <label className="text-sm">City / Area</label>
    <input className="border rounded w-full px-3 py-2" value={s.city} onChange={e=>setS({...s, city:e.target.value})}/>
  </div>
  <div>
    <label className="text-sm">Postcode</label>
    <input className="border rounded w-full px-3 py-2" value={s.postcode||''} onChange={e=>setS({...s, postcode:e.target.value})}/>
  </div>
</div>

	•	Availability section: replace the single text input with the calendar + month navigation:

<div>
  <div className="font-medium mb-2">Availability</div>

  <div className="flex items-center gap-2 mb-2">
    <button className="px-2 py-1 rounded bg-slate-200"
            onClick={()=>setCalMonth(m=>{ const nm=(m+11)%12; if(nm===11) setCalYear(y=>y-1); return nm; })}>←</button>
    <div className="font-medium">{new Date(calYear, calMonth, 1).toLocaleString('en-GB', { month:'long', year:'numeric' })}</div>
    <button className="px-2 py-1 rounded bg-slate-200"
            onClick={()=>setCalMonth(m=>{ const nm=(m+1)%12; if(nm===0) setCalYear(y=>y+1); return nm; })}>→</button>
  </div>

  <MonthCalendar
    year={calYear}
    month={calMonth}
    selected={s.availability?.unavailable||[]}
    onToggle={(iso, toAdd)=>{
      const cur = new Set(s.availability?.unavailable||[]);
      if (toAdd) cur.add(iso); else cur.delete(iso);
      setS({...s, availability:{ ...(s.availability||{}), unavailable:[...cur].sort() }});
    }}
  />

  <div className="mt-3">
    <label className="text-sm">Unavailable (comma-separated, optional manual edit)</label>
    <input className="border rounded w-full px-3 py-2"
      value={(s.availability.unavailable||[]).join(', ')}
      onChange={e=>setS({...s, availability:{...s.availability, unavailable:e.target.value.split(',').map(x=>x.trim()).filter(Boolean)}})}
    />
  </div>
</div>

	•	Locality map: add a new card below “Social & auto-posting” (or just above it):

{/* Locality / Map */}
<div className="bg-white border rounded-2xl p-5 shadow-sm space-y-2">
  <div className="font-semibold">Locality map</div>
  <div className="text-sm text-slate-600">
    Based on your <strong>Postcode</strong> or <strong>City</strong>. This is a simple embed for preview;
    exact address is never shown to owners.
  </div>
  <div className="rounded-xl overflow-hidden border" style={{height: '280px'}}>
    <iframe
      title="map"
      width="100%" height="100%" style={{border:0}}
      src={`https://www.google.com/maps?q=${encodeURIComponent((s.postcode||s.city||'Aylesbury'))}&output=embed`}
      loading="lazy" referrerPolicy="no-referrer-when-downgrade"
    />
  </div>
</div>

The embed uses a standard Google Maps search URL (no API key) to show the general area. You can switch to an OpenStreetMap embed later if you prefer.

	•	Save works as-is and will persist postcode and availability.unavailable.

⸻

4) WEB: SitterPublic (optional tiny tweak to show postcode)

In apps/web/src/screens/SitterPublic.jsx, in the header block where you show city, change:

<div className="text-slate-600">• {s.city}{s.postcode ? ` (${s.postcode})` : ''}</div>


⸻

What you’ll see
	•	Your Companion → Edit my profile now shows:
	•	“Becci” as the demo name (you can overwrite it).
	•	Postcode alongside City.
	•	A Month calendar where tapping days toggles unavailable dates (turn red).
The text field stays in sync for manual edits.
	•	A Map preview centred on postcode/city.
	•	Preview my public page will also show (HP20) next to the city if set.

⸻

Next small steps (when you’re ready)
	•	Calendar import: add a field “External iCal URL (.ics)” + basic parser to auto-mark busy days (we can add a tiny parser in the API; no third-party libs).
	•	Map radius: add a “service radius (km)” slider and draw it on the map or show a list of covered postcodes.
	•	Owner booking flow: from public profile, “Book Becci” → choose pet, service, dates (using the same calendar), price summary, then Stripe.